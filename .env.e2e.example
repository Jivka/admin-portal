# Copy this file to .env.e2e and fill in valid credentials.
# IMPORTANT: Use a System Admin account so it can create/edit/delete tenants.

# App URL Playwright should test against
PLAYWRIGHT_BASE_URL=https://localhost:5173

# E2E credentials (used by tests/tenants.spec.js)
E2E_EMAIL=systemadmin@example.com
E2E_PASSWORD=your-password-here
